<div class="bg-dark text-secondary px-4 py-5 text-center">
  <h1 class="display-5 fw-bold text-white">Welcome to <span class="tt tt-green">TracTrak</span></h1>
  <p class="fs-5 mb-4 text-white">Live, fast, FREE results from track meets. Tell your coaches and meet directors you want to see results faster using <span class="tt tt-green">TracTrak</span>.</p>
</div>
<%#--<div class="col-md-10 col-md-offset-1">--%>
<%#--<div class="panel panel-default">--%>
<%#--<div class="panel-header"><i class="fa fa-search"></i> Find a meet</div>--%>
<%#--<div class="panel-body">--%>
<%#--<input type="text" id="findEvent" autofocus style="width:100%" title="Find a Meet">--%>
<%#--</div>--%>
<%#--</div>--%>
<%#--</div>--%>
<div class="card mb-4 rounded-3 shadow-sm mt-2" id="current">
  <div class="card-header py-3">
    <h4 class="my-0 fw-normal">
      <img src="https://icongr.am/fontawesome/clock-o.svg" alt="clock"/> Current meets
    </h4>
  </div>
  <div class="card-body">
    <% if @current_meets.present? %>
      <ul class="list-group list-group-flush">
        <% @current_meets.each do |meet| %>
          <%= render partial: 'meet', locals: { meet: meet } %>
        <% end %>
      </ul>
    <% else %>
      <em>Nothing happening right now ...</em>
    <% end %>
  </div>
</div>
<div class="card mb-4 rounded-3 shadow-sm" id="upcoming">
  <div class="card-header py-3">
    <h4 class="my-0 fw-normal">
      <img src="https://icongr.am/fontawesome/calendar.svg" alt="Calendar"/> Upcoming meets
    </h4>
  </div>
  <div class="card-body">
    <% if @upcoming_meets.present? %>
      <ul class="list-group list-group-flush">
        <% @upcoming_meets.each do |meet| %>
          <%= render partial: 'meet', locals: { meet: meet } %>
        <% end %>
      </ul>
    <% else %>
      <em>Nothing planned right now ... maybe you'd like to use <span class="tt tt-green"><strong>TracTrak</strong></span> at your meet?</em>
    <% end %>
  </div>
</div>

<div class="card mb-4 rounded-3 shadow-sm" id="past">
  <div class="card-header py-3">
    <h4 class="my-0 fw-normal">
      <img src="https://icongr.am/fontawesome/calendar-check-o.svg" alt="Calendar with checkmark"/> Past meets
    </h4>
  </div>
  <div class="card-body">
    <ul class="list-group list-group-flush">
      <% if @recent_meets.present? %>
        <ul class="list-group list-group-flush">
          <% @recent_meets.each do |meet| %>
            <%= render partial: 'meet', locals: { meet: meet } %>
          <% end %>
        </ul>
      <% else %>
        <em>We haven't done anything ... yet.</em>
      <% end %>
    </ul>
  </div>
</div>
<div>
  <%= render partial: 'footer' %>
</div>